<div class="row">
    <div class="col-lg-12 col-md-12">
        <?php echo $this->stencil->setHeadline('hCalendar'); ?>
        <p><strong>hCalendar</strong> (short for <strong>HTML iCalendar</strong>) is a microformat standard for displaying a semantic (X)HTML representation of <?php echo $this->stencil->addLink('iCalendar', 'WD'); ?> format calendar information about an event, on web pages, using HTML <code>classes</code> and <code>rel</code> attributes.</p>
        <p>It allows parsing tools (for example other websites, or browser add-ons like <?php echo $this->stencil->addLink('Firefox', 'WB'); ?>'s Operator extension) to extract the details of the event, and display them using some other website, index or search them, or to load them into a calendar or diary program, for instance. Multiple instances can be displayed as timelines.</p>
    </div>
</div>